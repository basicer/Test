[
    {
        "description": "Uses devoured units to hatch `#{hatchType}` units. Each `hatch` spawns #{devourToHatchRatio} units per `devouredCount` devoured units, which are then used up.",
        "type": "function",
        "name": "hatch"
    }
]